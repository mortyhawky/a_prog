# a_prog

```bash
cd /a/prog

git init

//printf "# a_prog\n" > README.md
//
//git add README.md
//git commit -m "Initial commit"

gh auth status
gh auth login

gh repo create a_prog --public --source=. --remote=origin
git push --set-upstream origin main
```

```bash
gr
gs
ga
gc
gp
```
